export default {
  name: 'FlyTableColumn',
  props: {
    type: {
      type: String,
      default: 'normal'
    },
    width: String,
    prop: String,
    label: String,
    sortable: Boolean,
    filterable: Boolean
  },
  render (h) {
    return ('')
  }
}
